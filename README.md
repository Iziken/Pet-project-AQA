# Автотесты PomidorQA

[![CI](https://github.com/lebed52/pomidorqa-course-tests/actions/workflows/playwright.yml/badge.svg)](https://github.com/lebed52/pomidorqa-course-tests/actions/workflows/playwright.yml)

Набор автотестов на [PomidorQA](https://aiqa.su/pomidorqa) — сервис 25-минутных встреч для QA/IT-специалистов: регистрация, профиль с навыками, слоты доступности, каталог участников, бронирование и отмена встречи.

Продукт тестируется как чёрный ящик: исходного кода приложения у набора нет, доступен только живой стенд. Поэтому покрытие считается от функциональных требований, а не от строк кода.

## Что проверяет проект

Основной бизнес-риск продукта — встреча, о которой договорились два человека. Она не должна теряться, дублироваться и доставаться двоим одновременно. Вокруг этого построен набор:

- сквозной путь «зарегистрировался → заполнил профиль → открыл слот → нашёл в каталоге → забронировал → увидел встречу у обоих участников»;
- гонка за один слот: подтверждённой остаётся ровно одна бронь;
- освобождение слота после отмены и повторное бронирование другим участником;
- границы роли: гость смотрит каталог, но бронировать не может.

Дополнительно проверяются правила профиля, навыков, доступности слотов, каталога участников, бронирования, отмены и списка встреч.

## Архитектура

```text
tests/unit/      чистые функции — без сети и браузера
tests/api/       бизнес-правила и HTTP-контракт — без UI
tests/e2e/       пользовательские сценарии в браузере
tests/helpers/   тестовые данные, Arrange через API, cleanup
tests/pages/     Page Object: локаторы и действия одного экрана
docs/            матрица покрытия требований
```

Разделение жёсткое: в spec — сценарий и проверки, в helper — пользователь, в Page Object — экран. Локаторов в spec нет, `expect` в Page Object нет.

Уровень выбирается по риску, а не по привычке: правила брони дешевле проверять запросом, а не кликами, поэтому они живут на API-уровне. UI отвечает за то, что человек реально видит.

Оговорка про пирамиду: unit-уровень во внешней автоматизации проверяет собственный код набора, а не продукт. Широкое основание пирамиды здесь недостижимо, и это осознанный компромисс, а не недоработка.

## Как запустить

Команды — из `package.json` и `playwright.config.ts` этого репозитория:

```bash
npm ci
npx playwright install --with-deps chromium

npm run lint        # ESLint по tests/ — правила стабильности Playwright
npm test            # все проекты: unit → api → e2e
npm run test:unit   # только чистые функции
npm run test:api    # только мок-сервер бронирования
npm run test:e2e    # только браузерные сценарии на живом стенде
npm run report      # HTML-отчёт последнего прогона
```

Стенд по умолчанию — продакшен `https://aiqa.su/pomidorqa`. Переопределяется переменной:

```bash
POMIDORQA_BASE_URL=http://localhost:3000 npm run test:e2e
```

## CI Pipeline

GitHub Actions запускает проверки при `push` в `main`, Pull Request в `main` и вручную через `workflow_dispatch`.

### CI Flow

| Этап                       | Что запускается               | Условие                                      |
| -------------------------- | ----------------------------- | -------------------------------------------- |
| **1. Static Analysis**     | TypeScript + ESLint           | Запускается первым                           |
| **2. Tests & Performance** | Unit + API + E2E + Lighthouse | После успешного Static Analysis, параллельно |
| **3. Reporting**           | Telegram + DORA Metrics       | После завершения всех проверок               |

### CI Jobs

| Job                   | Проверяет                     | Команда                             |
| --------------------- | ----------------------------- | ----------------------------------- |
| **Lint & Type Check** | TypeScript + ESLint           | `npx tsc --noEmit` · `npm run lint` |
| **Unit Tests**        | Изолированные функции         | `npm run test:unit`                 |
| **API Tests**         | API и контракты данных        | `npm run test:api`                  |
| **UX Performance**    | Performance + Core Web Vitals | `lhci autorun`                      |
| **E2E Tests**         | Пользовательские сценарии     | `npm run test:e2e`                  |
| **Reports**           | HTML / JSON / JUnit           | Автоматически                       |
| **Notifications**     | CI status + DORA Metrics      | Telegram                            |

### Playwright Projects

`unit` → `tests/unit` · `api` → `tests/api` · `e2e` → `tests/e2e`

### Commands

```bash
npm test              # all Playwright tests
npm run test:unit     # unit
npm run test:api      # API
npm run test:e2e      # E2E
npm run typecheck     # TypeScript
npm run lint          # ESLint
npm run metrics       # test metrics
npm run report        # HTML report
npm run gate          # typecheck + lint + tests
```

### Reports & Artifacts

- **HTML** — Playwright Report
- **JSON** — `playwright-report/results.json`
- **JUnit** — `playwright-report/results.xml`
- **E2E artifacts** — screenshot, trace, video on failure
- **GitHub Artifact** — `playwright-report/`, retention **14 days**
- **Telegram** — CI status, duration, commit, diff stats and links

## Покрытие требований

[Матрица покрытия требований](docs/coverage-matrix.md)

| Статус         | Требований |
| -------------- | ---------: |
| `automated`    |         25 |
| `partial`      |         11 |
| `не покрыт`    |         12 |
| `out of scope` |          2 |
| **Всего**      |     **50** |

Не покрытые (12) — правила, которые в этой ветке ещё никто не проверяет:

| ID    | Требование                                                    |
| ----- | ------------------------------------------------------------- |
| R4.1  | Имя, email и пароль обязательны                               |
| R4.2  | Пароль не короче 8 символов                                   |
| R4.3  | Профиль после регистрации: имя из формы, пояс `Europe/Moscow` |
| R4.4  | Второй аккаунт на тот же email не создаётся                   |
| R5.6  | Профиль виден другим участникам                               |
| R6.5  | Участник удаляет свой навык в любой момент                    |
| R7.4  | Свой свободный слот можно удалить                             |
| R7.5  | Забронированный слот удалить нельзя                           |
| R9.2  | Забронированные слоты не показываются                         |
| R10.1 | Свой слот забронировать нельзя                                |
| R10.5 | Закрытие окна подтверждения не создаёт бронь                  |
| R11.2 | Отмена запрещена позднее чем за 2 часа до начала              |

Частично покрытые (11): R3.6, R5.1, R5.5, R6.4, R7.2, R7.3, R8.3, R9.1, R10.3, R11.3, R12.3 —
у каждого покрыта наблюдаемая часть правила, полный список «что покрыто / чего не хватает» —
в [матрице](docs/coverage-matrix.md).

Главная метрика — определённый статус у каждого требования, а не «100% покрытие» без привязки к требованиям.

В детальной матрице статус `automated` означает, что есть автотест, который падает при нарушении поведения; `partial` — что покрыта только наблюдаемая часть правила; `known defect` — что требование не выполняется продуктом и тест помечен `test.fail()`; `out of scope` — что состояние нельзя проверить через доступные интерфейсы продукта.

## Метрики набора

| Метрика                       | Значение                                           |
| ----------------------------- | -------------------------------------------------- |
| Тестов всего                  | **38** (unit 10 / api 8 / e2e 20)                  |
| Время полного прогона         | ~1.7 мин, один worker                              |
| Flaky rate                    | 2 падения на 170 повторных e2e-прогонов (см. ниже) |
| E2E с Arrange через API       | 7 из 7 файлов                                      |
| E2E с гарантированным cleanup | 7 из 7 файлов                                      |

Как получены:

- количество тестов — `npx playwright test --list` по каждому проекту;
- время — полный прогон `npx playwright test --workers=1` против живого стенда;
- flaky rate — десятикратные прогоны `--repeat-each=10`: `booking-flow` + `booking-cancel` (оба теста отмены) — 50/50,
  `profile-flow` — 90/90, `auth-flow` — 10/10, `guest-access` — 28/30 (оба падения — таймаут
  теста 30с на нагруженном стенде, не логика);
- Arrange/cleanup — по коду: каждый e2e-файл регистрирует пользователей через служебный API
  и удаляет их в `afterEach`/`finally` даже при падении теста.

| Статус         | Требований | Доля |
| -------------- | ---------: | ---- |
| `automated`    |         22 | 44%  |
| `known defect` |          1 | 24%  |
| `partial`      |         12 | 28%  |
| `out of scope` |          2 | 4%   |

## Известные дефекты

Расхождения требований и продукта не спрятаны ради зелёного отчёта. Тесты на них написаны по требованию и помечены `test.fail()`: пока дефект жив, ожидаемый результат — падение, а когда продукт починят, Playwright сообщит `expected to fail, but passed`.

| ID   | Требование                                                 | Что происходит на самом деле                                                                                        |
| ---- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| KD-3 | R8.3 — поиск фильтрует по навыкам из раздела «могу помочь» | Фильтр ищет по навыкам обоих типов. По запросу находится также участник, который только хочет разобрать этот навык. |

Тест `test.fail()` для KD-3 (`e2e/known-defects`) живёт на ветке регрессии; в этой ветке R8.3
помечен `partial` — позитивный поиск покрыт (`e2e/catalog-search`), исключение навыков
«хочу разобрать» не проверено.

KD-3 относится к требованию R8.3. Требование ограничивает поиск навыками из раздела «могу помочь», но фактический фильтр проходит по всем навыкам участника.

В матрице отдельно отмечено, что прежний зелёный тест фиксировал реализацию как правильную и тем самым скрывал расхождение. Он заменён на `test.fail()` по требованию.

## Закрытые дефекты

Два расхождения после аудита были исправлены в продукте. Тесты `e2e/slot-timezone` и
`e2e/cancel-window` живут на ветке регрессии и в эту ветку ещё не перенесены.

### KD-1 — часовой пояс профиля не применялся к слотам → исправлено

Требование R5.5: время слотов показывается в часовом поясе владельца.

До исправления хост создавал слот на 12:00 и видел 12:00, а гость на странице хоста видел 15:00. После исправления часовой пояс владельца учитывается при создании и отображении слота — в собственных слотах, календаре на странице участника и в «Моих встречах».

Тест: `e2e/slot-timezone`.

Отдельно матрица отмечает, что unit-тест `formatSlotTime` не доказывал корректность PomidorQA, поскольку проверял учебную функцию из `src/pyramid/slots.ts`, а не код продукта.

### KD-2 — отмену можно было сделать в последний момент → исправлено

Требование R11.2: отмена запрещена позднее чем за 2 часа до начала.

До исправления функция проверяла только то, что отменяющий является участником встречи, но не проверяла время до начала. После исправления функция возвращает `cancel_window_closed`, если до начала осталось меньше двух часов, а страница «Мои встречи» показывает причину отказа.

Тест: `e2e/cancel-window`.

## Ограничения black-box покрытия

Два требования имеют статус `out of scope`.

### R7.1 — фиксированная длительность слота 25 минут

`end_time` не выводится в интерфейс. Надпись «25 минут» в интерфейсе является текстом макета, а не вычисленной длительностью.

Для проверки фактической длительности нужен доступ к БД или API, поэтому через доступный black-box интерфейс это правило доказать нельзя.

### R9.3 — прошедшие слоты не показываются

Продукт не позволяет создать слот в прошлом, а ждать естественного перехода существующего слота в прошлое для регрессионного прогона непрактично.

Для проверки такого состояния требуется подготовка данных в БД.

## Роль AI-агентов

В рабочем пространстве проекта настроен агент-писатель (`.zcode/skills/ai-avtomatizator`):

- пишет и правит тесты по правилам `CODEX.md`: spec/helper/page, `test.step`, уникальные данные, запрет `waitForTimeout` и `force`;
- локаторы для новых E2E собирает по реальному DOM живого стенда, а не по исходникам;
- обязан прогнать ESLint и десятикратный `--repeat-each` нового теста на флапы перед отчётом;
- уборка данных — служебным API стенда, аккаунты удаляются даже при падении теста.

Что агент не решает: выбор уровня проверки, приоритет по риску, статус расхождения
требований и продукта, approve и merge. Последнее слово за человеком.

## Личный вклад

- спроектировал матрицу покрытия из **50 функциональных требований**;
- разнёс требования по уровням `unit / api / e2e`;
- написал и поддерживаю сценарии для ключевых пользовательских путей, включая бронирование и отмену;
- покрыл сценарий гонки за один слот, где подтверждённой остаётся ровно одна бронь;
- закрыл границы роли гостя: каталог и слоты доступны, бронь и приватные разделы — нет;
- закрыл вход/выход: сессия держится после перезагрузки и закрывается на сервере после выхода;
- закрыл отмену встречи обеими сторонами: и гостем, и хостом — с проверкой, что после reload отмену видят оба;
- оформил расхождение R8.3 как `known defect` KD-3 с тестом `test.fail()` (ветка регрессии);
- нашёл и довёл до исправления два расхождения требований и продукта: KD-1 по часовому поясу и KD-2 по окну отмены;
- зафиксировал ограничения black-box подхода для правил R7.1 и R9.3;
- сформировал набор из **10 unit, 8 API и 20 E2E тестов** (38 всего) с Arrange через API и гарантированным cleanup во всех e2e-файлах.
